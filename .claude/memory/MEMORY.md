# Memory Index

> Routing index for the vault. One line per memory/domain tile, newest first.
> This is a fresh install; entries appear as sessions are captured and domains ingested.
> Keep this file concise — offload verbose domain schemas to `domains/<slug>.json`.

<!-- entries appended here as the brain grows -->
